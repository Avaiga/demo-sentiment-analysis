[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scipy>=1.15.1",
    "taipy>=4.0.2",
    "torch>=2.5.1",
    "transformers>=4.48.1",
]
